import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/* Context API - We mainly use it to transfer data between multiple components 
without following Parent  Child Architecture.
React Context API allows you to easily access data at different levels of the
component tree, without passing prop to every level */

ReactDOM.render(<App/>,document.getElementById('root',()=>("Server Started"))
);


